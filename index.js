const express = require('express');

const server = express();

server.use(express.json());

const cursos = ['FuççStack Master', 'Desenvolvimento de Games', 'Viver de Youtube'];











server.listen(3000);
